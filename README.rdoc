== AUTOMANDADO

This is a POC about a delivery system.

Includes:

* Ruby version

* Autentication with Devise

* Manage permissions with Cancan

* Manage roles with role_model

* CRUD for admin

* Web surfing for users

* Delivery via email

This is in currently developmet.

== Setting up for first running:

# Run migrations
# Run seeds.rb to create the default admin user credentials.
# Login as admin and change credentials
# Setup the application in the Admin Dashboard